# Core web framework and ASGI server
fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic>=2.11.0,<3.0.0
pydantic-settings==2.5.2

# HTTP client with HTTP/2 support
httpx[http2]==0.27.2

# Security and guardrails - llm-guard and its dependencies
llm-guard==0.3.16
bc-detect-secrets==1.5.43
faker>=37,<38
fuzzysearch>=0.7,<0.9
json-repair==0.44.1
nltk>=3.9.1,<4
presidio-analyzer==2.2.358
presidio-anonymizer==2.2.358
regex==2024.11.6
tiktoken>=0.9,<1.0
torch>=2.4.0
transformers==4.51.3
structlog>=24

# YAML configuration
pyyaml==6.0.2

# Performance optimizations
orjson==3.10.7  # Fast JSON serialization
uvloop==0.20.0  # Fast event loop (Unix only)

# Caching and performance
cachetools==5.5.0
redis[async]==5.1.1  # Redis client for distributed caching
hiredis==3.0.0  # C parser for Redis (performance boost)

# Security (presidio-anonymizer requires cryptography<44.1)
cryptography>=43.0.0,<44.1


