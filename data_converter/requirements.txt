# Document Converter Requirements

# For DOCX conversion (Windows only - requires MS Word)
docx2pdf>=0.1.8

# For XLSX conversion
openpyxl>=3.1.2

# For PPTX conversion
python-pptx>=0.6.23

# PDF generation and manipulation
reportlab>=4.0.7
pypdf>=5.1.0  # For RAG metadata injection in LibreOffice/Python converters

# Optional: Advanced layout recognition (install separately if needed)
# Uncomment to enable Docling converter for enhanced Excel/document layout analysis
# docling>=2.63.0  # Advanced layout recognition for complex tables

# For Microsoft Office 365 integration (Windows only)
pywin32>=311; platform_system=="Windows"

# Progress bar (v2.5)
tqdm>=4.66.0

# Alternative: For cross-platform conversion (requires LibreOffice installation)
# LibreOffice must be installed separately on the system
# Windows: https://www.libreoffice.org/download/download/
# Linux: sudo apt-get install libreoffice
# macOS: brew install libreoffice
